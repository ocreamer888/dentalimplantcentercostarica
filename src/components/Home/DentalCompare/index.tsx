"use client";
import ImageSlider  from "@/components/ImageSlider";

const DentalCompare = () => {
  return (
    <section className="rounded-3xl overflow-hidden my-20">
      <ImageSlider />
    </section>
  );
};

export default DentalCompare;
